import { Loader } from '@mantine/core'; 

// Spinner-komponenten, der viser en loader (indlæsningsindikator)
export default function Spinner() {
  return <Loader color="blue" />;  // Returnerer Loader med blå farve som spinner
}